# AddToEnd
hello my name is yehonatan in last 4 days i start learning c is my first project
​To compile this project, you need a compiler that supports C23 (e.g., GCC 13+ or Clang 17+).
Use the following command:
gcc -std=c23 main.c -o linked_list
